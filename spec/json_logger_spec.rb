require "spec_helper"

RSpec.describe Xyeger do
  it "has a version number" do
    expect(Xyeger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
